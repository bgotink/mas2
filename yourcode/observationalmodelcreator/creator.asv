function [observation ] = creator(transition,nrStates,nrActions)

observation = zeros(nrStates,nrActions,17);

for s=1:nrStates
    for a=1:nrActions
           [x,y] = toXY(s);
           observation = identifyObservation(
           observation(s,a)=identifyObservation(  
    end
end


end

